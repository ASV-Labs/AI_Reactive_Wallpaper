# Dependencies / tooling (none required for disk plugins)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
package-lock.json
yarn.lock
pnpm-lock.yaml

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo
*~

# Env / secrets (never commit)
.env
.env.*
!.env.example
*.pem
*.key
credentials*
secrets*

# Build / cache
dist/
build/
.cache/
coverage/
*.log
tmp/
temp/

# Local Hermes install copies
.hermes-package.json

# rebuild helper (logo is embedded in plugin.js)
assets/logo.b64.txt
